// import { fromJS } from 'immutable';
// import { selectOrderInvoiceDetailPageDomain } from '../selectors';

describe('selectOrderInvoiceDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
